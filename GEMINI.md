# Free Book Management System (วิทยาลัยเทคนิคอุดรธานี)
## ระบบจัดการรายการหนังสือเรียนฟรี ระดับ ปวช.

### 📌 ข้อมูลโปรเจกต์
- **ลักษณะงาน:** Web Application แบบ Client-side + Local Storage / Firebase (สำหรับ Real-time Cloud Sync)
- **เครื่องมือที่ใช้:** HTML5, CSS3, Vanilla JavaScript (`app.js`)
- **การจัดการข้อมูลภายนอก:** รองรับการดึงและนำเข้าข้อมูลผ่านไฟล์ Excel / CSV
- **สคริปต์เสริม (Python):** มีสคริปต์สำหรับอ่าน, แยกวิเคราะห์ และดัมพ์ข้อมูลโครงสร้างของไฟล์ Excel (เช่น `dump_excel.py`, `inspect_excel.py`) เพื่อใช้อ้างอิงในการพัฒนา

### 🗂️ โครงสร้างไฟล์ที่สำคัญ
- `index.html`: โครงสร้างหน้าเว็บ (UI), แถบนำทาง (Tabs), และกล่องข้อความตอบโต้ (Modals)
- `styles.css`: ไฟล์จัดการความสวยงามและธีมของเว็บ
- `app.js`: ไฟล์ลอจิกหลัก ควบคุมการคำนวณ, เรนเดอร์ตาราง, การกรองข้อมูล, การส่งออก PDF, และเชื่อมต่อ Firebase
- `guide.html`: คู่มือการพัฒนาและการใช้งานระบบ
- `รายการสั่งหนังสือ วิทยาลัยเทคนิคอุดรธานี2569.xlsx`: ไฟล์ข้อมูลต้นฉบับสำหรับอ้างอิง

### 🛠️ ฟีเจอร์หลักของระบบ
1. **Dashboard:** สรุปยอดรวม (งบประมาณ, จำนวนเล่ม, จำนวนวิชา, แผนก/สำนักพิมพ์) พร้อมกราฟแสดงผล
2. **Master List:** ตารางจัดการข้อมูลทั้งหมด รองรับการเพิ่ม ลบ แก้ไข นำเข้า และส่งออกข้อมูล
3. **Department View:** สรุปยอดสั่งซื้อแยกตามแผนกวิชา และเลือกระดับชั้น (ปวช.1 - ปวช.3)
4. **Publisher View:** สรุปยอดสั่งซื้อแยกตามค่ายสำนักพิมพ์
5. **Reports:** สรุปงบประมาณและแบบฟอร์มเอกสาร สอศ. สำหรับพริ้นท์/สร้าง PDF
6. **Data Management & Cleanup:** ระบบ Admin สำหรับเพิ่มปีการศึกษา และเลือกลบข้อมูลเก่า (เลือกลบได้ทั้งระดับปีการศึกษา และแยกลบเฉพาะบางภาคเรียน)

### 🚀 อัปเดตล่าสุด / สถานะปัจจุบัน
- [x] เพิ่มระบบเลือก **"ภาคเรียน" (Semester)** ในหน้าต่างลบข้อมูล/ล้างข้อมูลเก่า (Purge Data) เพื่อไม่ให้ต้องลบข้อมูลทิ้งทั้งปีการศึกษา หากต้องการลบแค่บางเทอม
- [x] ปรับปรุงระบบ **Backup** ข้อมูลก่อนลบ ให้รองรับการสำรองข้อมูลแยกตามเทอม หรือทั้งปีการศึกษาได้

### 📝 กฎการทำงานร่วมกับ AI (Workspace Rules)
1. การแก้ไข UI หรือ DOM ใดๆ ต้องตรวจสอบ ID และ Class ให้ตรงกับใน `index.html` และ `app.js` 
2. ทุกครั้งที่เพิ่มฟีเจอร์ใน `app.js` ควรตรวจสอบผลกระทบกับการบันทึกข้อมูลลง `localStorage` และ `Firebase` 
3. คอมเมนต์โค้ดและคำอธิบาย ควรใช้เป็น **ภาษาไทย** เพื่อให้ง่ายต่อทีมงานวิทยบริการฯ ในการดูแลรักษา
